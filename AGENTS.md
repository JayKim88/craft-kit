<!-- BEGIN:env-agent-rules -->
# Environment Notes

<!-- HINT: If your framework / runtime differs from common AI training data (typically 2024-2025),
     record those differences here. AI must read this before writing code so it doesn't
     fall back on stale assumptions.

     Examples:
     - "This is Next.js 16 — async dynamic APIs (`params`, `searchParams`, `cookies()`, `headers()` return Promises). Turbopack is the default."
     - "Python 3.13 — typing changes, `imp` removed."
     - "Java 21 — record patterns, sequenced collections."
     - "Go 1.22 — for-range integer support."

     If nothing notable, leave a single line: "No special concerns (standard environment)."
-->

_TO FILL — environment-specific gotchas, or "No special concerns (standard environment)."_

<!-- END:env-agent-rules -->
