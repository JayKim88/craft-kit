# Project Spec — Original (immutable)

> Preserves the original project spec as-is. **Do not edit this document.**
> Our interpretations and decisions go in [PLAN.md](PLAN.md) and [DESIGN.md](DESIGN.md).

---

<!-- SPEC PASTE START -->
<!--
Paste the original project spec here verbatim.
Preserve formatting: if given as markdown, keep markdown; if Word/PDF, transcribe to text — never paraphrase.
-->

[[ paste original project spec here ]]

<!-- SPEC PASTE END -->

---

## Requirements (detail)

<!-- HINT: Define 5-7 evaluation categories. Assign §1, §2, ... — these map 1:1 to CHECKLIST.md and [§N] commit tags.

     Two modes:
     - If the spec provides explicit scoring (pts): transcribe each criterion verbatim, including point weights.
       Proc-4 will run in score-simulation mode.
     - If no scoring defined (most projects): define categories yourself from the spec scope.
       List checkpoints under each. Proc-4 runs in coverage-check mode.

     The 6-category template below is a common starting shape. Adjust count and names to fit.
-->

### 1) Requirements understanding & problem definition

<!-- HINT: List what "done well" looks like for this category.
     If spec provides scoring levels (e.g. Low/High proficiency), transcribe verbatim and add `(`<points>` pts)` to the heading. -->

| Checkpoint | Notes |
|---|---|
| ... | ... |

### 2) Design & code structure

| Checkpoint | Notes |
|---|---|
| ... | ... |

### 3) Stability & exception handling

| Checkpoint | Notes |
|---|---|
| ... | ... |

### 4) `<Domain-specific category — UI/UX or data correctness or ...>`

| Checkpoint | Notes |
|---|---|
| ... | ... |

### 5) Documentation & explainability

| Checkpoint | Notes |
|---|---|
| ... | ... |

### 6) Git / work trail

| Checkpoint | Notes |
|---|---|
| ... | ... |

<!-- HINT: Add §7, §8 if the spec has more categories. Remove §6 if Git history isn't a separate criterion. -->

---

## Evaluation questions (optional)

<!-- HINT: If the spec includes evaluation questions in advance, paste them. If not, delete this section. -->

_TO FILL or DELETE_

---

## Rationale — stated intent behind the spec (optional)

<!-- HINT: If the spec states its intent or evaluation focus, transcribe it here. Delete if absent.
     This section is high-value when present — it reveals where emphasis is highest. -->

_TO FILL or DELETE_
